# GitHub App credentials (see README "Register the GitHub App")
APP_ID=
WEBHOOK_SECRET=
# Path to the private key .pem downloaded from the App settings page
PRIVATE_KEY_PATH=./greenlight.private-key.pem

# smee.io channel URL (open https://smee.io/new to mint one)
SMEE_URL=

# Fireworks API key for test-plan generation AND browser step execution/judging
# (https://app.fireworks.ai/settings/users/api-keys)
FIREWORKS_API_KEY=
# Any Fireworks model id (default: accounts/fireworks/models/kimi-k2p6)
# GREENLIGHT_MODEL=
# Optional: a separate model for driving/judging browser steps (Stagehand
# act/extract). Falls back to GREENLIGHT_MODEL, then the default.
# GREENLIGHT_EXECUTOR_MODEL=
# Testing: set to 1 to skip Fireworks and drive the pipeline from
# src/mockPlan.ts (deterministic plan for validating preview + browser). Unset
# = real model.
# GREENLIGHT_MOCK_PLAN=1

# Vercel preview (Phase 3). Both optional.
# Secret from Vercel → Settings → Deployment Protection → "Protection Bypass for
# Automation". Only needed if previews are protected; leave blank for public ones.
# VERCEL_AUTOMATION_BYPASS_SECRET=
# Max ms to wait for a preview to build before staying silent (default 300000).
# PREVIEW_TIMEOUT_MS=

# Browserbase for browser execution (Phase 4). https://browserbase.com
# Leave both blank to skip execution (the pipeline stops after preview).
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
# Max concurrent browser sessions (free tier ~3). Default 3.
# MAX_CONCURRENT_SESSIONS=
# Hard cap on a single session's lifetime, ms. Default 900000 (15 min).
# SESSION_TIMEOUT_MS=

# Session recording (rrweb). Set to a directory to record the browser run and
# write a self-contained replay.html there; leave blank to record nothing.
# Browserbase records its own sessions, so this is mainly for local runs and the
# GitHub Action (where the workflow sets it and uploads the result).
# GREENLIGHT_REPLAY_DIR=./greenlight-replay
# Images are embedded in the replay by default, so it renders for viewers
# without preview access and after the preview is torn down. Set to 0 to link
# them by URL instead — much smaller files, needs a reachable preview to view.
# GREENLIGHT_REPLAY_INLINE_IMAGES=0

VERCEL_AUTOMATION_BYPASS_SECRET=

PORT=3000

# Run the local browser headless. Unset = headed, so you can watch a plan run.
# GREENLIGHT_HEADLESS=1
