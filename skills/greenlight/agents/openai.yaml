interface:
  display_name: "Greenlight"
  short_description: "Initialize browser setup or test a PR"
  default_prompt: "Use $greenlight init to provide your local setup steps, or $greenlight with a PR URL and preview URL to run checks."

policy:
  allow_implicit_invocation: false
