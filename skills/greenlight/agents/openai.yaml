interface:
  display_name: "Greenlight"
  short_description: "Initialize browser setup or test a PR"
  default_prompt: "Use $greenlight init with a repository URL to draft local setup, or $greenlight with a PR URL and preview URL to run checks."

policy:
  allow_implicit_invocation: false
